<h2>All Categories</h2>

<% @categories.each do |category| %>
  <p><%= link_to category.name, admin_category_path(category.id) %>: <%= link_to "Delete", admin_category_path(category.id), method: :delete, class: "#{category.name}-delete" %></p>
<% end %>
